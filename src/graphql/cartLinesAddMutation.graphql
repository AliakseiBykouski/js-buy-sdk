mutation ($cartId: ID!, $lines: [CartLineInput!]!) {
  cartLinesAdd(cartId: $cartId, lines: $lines) {
    userErrors {
      ...CartUserErrorFragment
    }
    cart {
      ...CartFragment
    }
  }
}
